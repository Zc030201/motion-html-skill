interface:
  display_name: "Motion HTML"
  short_description: "Generate cinematic animated HTML effects from prompts or references."
  brand_color: "#276EF1"
  default_prompt: "Use $motion-html-skill to create a high-end interactive HTML animation from this visual brief."

policy:
  allow_implicit_invocation: true

